# release/ — gate outputs (CI-written, do not hand-edit)

`run-manifest.json` (the object the publish job asserts), `heldout-eval.json`,
`publication-gate.json`, signatures, and the rendered model card land here.
Produced only inside the model-publish-gate workflow; committed copies are
snapshots for review, not release inputs.
